use na::Vector3;

extern crate nalgebra as na;

// default precision used for calculation
pub type Fp = f64;

pub type Vector3f = Vector3<Fp>;

pub const IMG_WIDTH: u32 = 300;
pub const IMG_HEIGHT: u32 = 200;
